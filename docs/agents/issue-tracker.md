# Issue tracker: GitHub

Specs and tickets live in GitHub Issues. Use `gh` to create, read, label, comment on, and close issues. Infer the repo from its git remote.

PRs as a request surface: no.

When publishing a spec or ticket, create an issue. Use GitHub's native issue dependencies for blockers where available; otherwise put `Blocked by: #<number>` in the ticket.
